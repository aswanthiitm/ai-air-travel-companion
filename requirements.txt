# Backend (Milestones 1-5). UI/API dependencies below.
pandas>=2.2
pytest>=8.0

# API layer (Milestone 6)
fastapi>=0.115
uvicorn[standard]>=0.32
httpx>=0.27

# Reasoning layer (optional: agent mode needs GROQ_API_KEY; offline fallback otherwise)
langchain-core>=1.0
langchain-groq>=1.0
