# Backend (Milestones 1-5). UI/API dependencies below.
pandas>=2.2
pytest>=8.0

# API layer (Milestone 6)
fastapi>=0.115
uvicorn[standard]>=0.32
httpx>=0.27

# Reasoning layer (optional: agent mode needs an LLM API key; deterministic
# fallback otherwise). langchain-groq powers GROQ_API_KEY; langchain-openai
# powers CEREBRAS_API_KEY and OPENROUTER_API_KEY (both use the OpenAI-
# compatible chat completions API via a custom base_url).
langchain-core>=1.0
langchain-groq>=1.0
langchain-openai>=1.0
